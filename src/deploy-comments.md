# Deploy Comments - Pending Changes Log

Lightweight, in-repo record of incremental changes accumulated since the last src/deploy-version bump. Updated every session (see Session Runbook -> End of Session, step 3). When the pending list below represents a complete batch (roughly 2-3 requirements), consolidate it into one new src/deploy-version line and reset this section.

Tracking starts at Session 24 - everything before this point lives in the Session Logs database and the Migration \& Build Log's Recently Closed archive, not duplicated here.

## Pending (since v1.0.12)

* Session 30: Buyer-entitlement gate + signed-URL delivery for Blob-backed media — gate /reader/$productId on purchase (redirect non-buyers to /product/$productId), new /api/media/$productId route streams private Blob media only after a hasPurchased() check, MediaPlayer.tsx routes real uploads through the gated endpoint (sample fallback unchanged). Range/206 not implemented (flagged v1 gap); buyer happy-path (real purchase + real Blob upload) not yet live-tested due to empty dev data.
* Session 30: Real Stripe Checkout Sessions + webhooks — stripeCheckout.ts rewritten from full localStorage simulation to real Stripe Checkout Sessions + a signature-verified webhook; fixes client-controlled cart pricing (server recomputes price from catalog + live promo settings) and the completeCheckout() duplicate-insert race (DB unique index + onConflictDoNothing). Extracted session-independent helpers (clearCartForUser, creditAffiliateForPurchaseForBuyer, insertDownloadIfNew) so the webhook can fulfill purchases without a request session. Session 31 update: a real Stripe sandbox account was set up locally and a full checkout was live-tested end to end (real downloads row, correct stripe\_payment\_intent\_id, webhook returned 200) — Gap B (local live testing) is resolved. Still not yet configured in Vercel (Production/Preview lack STRIPE\_SECRET\_KEY/STRIPE\_WEBHOOK\_SECRET and there's no live webhook endpoint registered in Stripe yet), so this remains local-dev-only until that's done.
* Session 30: Wire Buy Now on the real product page — extended createCheckoutSession()/initiateCheckout() to support a single-item purchase independent of the persisted cart; wired a real Buy Now button into product.$productId.tsx and QuizResults.tsx via a shared buyNow() helper. Session 31 update: live-tested for real — Buy Now on one item correctly left an unrelated item already in the cart untouched, confirmed directly in Neon. QuizResults.tsx's button still not click-tested live (blocked by an existing premium-tier LicenseGate, unrelated to this work).
* Session 31: Fix — Buy Now was silently wiping unrelated cart items on fulfillment. webhook.ts's handleCheckoutSessionCompleted() called clearCartForUser(userId) unconditionally after every purchase, deleting the buyer's entire persisted cart rather than just what was bought — invisible for a full-cart checkout (everything in the cart was purchased anyway) but a real data-loss bug for the single-item Buy Now path added in Session 30 (a buyer with an unrelated item already in their cart lost it on an unrelated Buy Now purchase, no error surfaced). Fixed by scoping cart-clearing to the productIds actually charged for: new removeCartItemsForUser(userId, productIds) helper in cart.ts replaces the old unconditional clear, fed from the same full.line\_items already used for fulfillment (no second fetch). clearCartForUser removed entirely — confirmed via repo-wide grep as its only caller. Live-tested this session (see the Checkout Sessions/Wire Buy Now updates above) — confirmed fixed for real, not just via code diff.
* Session 31: Real refunds via Stripe Refunds API — refunds.ts's dbResolveRefundClaim no longer scopes by the admin's own userId (real bug: approving a refund silently no-op'd for any claim that wasn't the admin's own account); now gated by id + status='pending' as an idempotency guard, calls stripe().refunds.create() before flipping the claim's status (a failed Stripe call leaves the claim retryable, never falsely approved). New markSingleDownloadRefunded (downloads.ts, per-download-scoped, replacing the now-dead session-scoped markDownloadsRefunded) and clawbackLoyaltyPointsForBuyer (loyalty.ts, unconditional, can go negative — a clawback isn't a voluntary redemption) wired into the approval path alongside the existing voidAffiliateLedgerForDownload. Also fixed a second real bug caught during this work: the old approval flow credited loyalty points back to the admin's own account instead of the buyer's, via a caller-session-scoped helper being reused server-side. RefundClaimsManager.tsx's approve action now only calls approveRefundClaim; the "restore points" toggle was removed since it no longer controlled anything real. Not yet live-tested — pushed but the five live-sandbox verification tests (partial refund, retry/idempotency, cross-buyer approval, negative-balance clawback, reject-path no-op) haven't been run yet.
* Session 32: Added a temporary, disposable "TEST: Checkout Full Cart" button to product.$productId.tsx for end-to-end verification of the multi-item Stripe checkout plumbing (initiateCheckout() with no args, exercising the full-cart path rather than Buy Now's single-item path) — dashed-border, clearly labeled TEMP, disabled when the cart is empty, mirrors handleBuyNow's loading/error pattern. Not production UI; to be removed once plumbing is confirmed.
* Session 50: Documentation-only placeholder — added a comment block above handleCheckoutSessionCompleted in webhook.ts noting its current auto-capture assumption and what a future manual-capture module (RaaS, physical rentals, ticketing) would need to check (PaymentIntent status: requires\_capture vs succeeded) before granting access, plus the eventual capture-sweep job that would trigger that transition. No functional change.
* Session 50: Wired Sign in with Apple into the existing Better Auth setup — apple() social provider in auth.ts, fully opt-in on APPLE\_CLIENT\_ID/APPLE\_TEAM\_ID/APPLE\_KEY\_ID/APPLE\_PRIVATE\_KEY (all four required together once APPLE\_CLIENT\_ID is set, otherwise the button just doesn't render), client secret generated via Better Auth's documented generateAppleClientSecret JWT helper rather than hand-signed. Button added to AdminLogin.tsx (the app's only sign-in/sign-up UI) styled per Apple's HIG (official black mark); admin.tsx gained a small ?appleAuth=1 bridge on the OAuth callbackURL since the full-page redirect back can't call the existing sessionStorage-gate handler in memory the way the password path does. Confirmed against better-auth's own source (not assumed) that the anonymous-to-real-user onLinkAccount merge fires for Apple the same as email/password (its hook matches any /callback path, provider-agnostic), and that the OAuth flow has real CSRF/state protection — a random state value stored server-side plus a signed cookie, both cross-checked at callback, same mechanism for every social provider. Not click-tested: Apple Sign In has no localhost/non-HTTPS support at all, so this is verified against source/config only, pending a real run on a Vercel preview URL. A partial/incomplete Apple env-var set no longer crashes the app at boot (initial implementation did; fixed same session) — it now logs which var(s) are missing and disables Apple sign-in only, leaving every other route unaffected.
* Session 51: Catalog storefront read migration, Tier C (final) — repointed the last five data/catalog.ts consumers (AIChatbot.tsx, QuizResults.tsx, challenge.tsx, OffboardingCenter.tsx, admin.tsx) onto the Postgres-backed db/queries.ts versions, same pattern as Tiers A/B, plus a sixth hidden consumer (data/membership.ts's generateUpsellOffers, reached via a relative "./catalog" import the alias-based repo grep didn't catch) that would otherwise have broken at runtime. admin.tsx's bulk promo-override editor (PromotionsAdminSection) no longer does a full-array localStorage.setItem("omnimedos\_catalog", ...) overwrite on save; added updateCatalogPromoOverride to queries.ts (mirrors updateCatalogAccess — field-scoped, admin-gated) and the save handler now diffs bulkCatalogs against an originalOverrides snapshot so it only writes promoOverride on rows that actually changed, instead of risking a stomp on concurrent edits to other fields on the same rows. Deleted getCatalogItems/saveCatalogItem/saveAll/updateCatalogStatus/updateCatalogRating/STORAGE\_KEY from data/catalog.ts, and also getLiveCatalogItems/getActiveCatalogItems/getComingSoonCatalogItems/createCatalogItem/deleteCatalogItem after confirming via repo-wide grep they had zero remaining runtime importers — catalog.ts now exports only the CatalogItem/CatalogStatus types. tsc --noEmit is clean (only the pre-existing translations-test.ts parse errors show, as expected); separately re-ran with that file excluded to catch real regressions, since its parse failure otherwise masks semantic diagnostics for the whole project — that run is what caught the membership.ts issue above. Live-verified in the local dev server: the homepage (SpotlightLayout/ComingSoonSection's shared getCatalogItems chain) rendered real Neon catalog data end to end. Not live-verified: QuizResults.tsx and challenge.tsx both sit behind an existing premium-tier LicenseGate that's locked in this dev environment (pre-existing, unrelated to this change); AIChatbot.tsx isn't mounted anywhere in the current app (pre-existing, unrelated); admin.tsx wasn't exercised at all (no admin credentials, per standing decision). Those were verified by code review and the tsc pass only.
* Session 52: Storefront layout + style presets migration to Neon — added storefront\_layout and style\_presets tables to schema.ts, both single global "global"-id rows shaped exactly like promoSettings/licenseSettings; style\_presets folds border/typography and every AnnouncementConfig field into one row, matching how they already share one file and one admin component today. Generated and applied migration 0017\_true\_rictor.sql (two additive CREATE TABLEs, nothing dropped or altered) against the real Neon DB. Applying it needed a workaround: `export $(grep DATABASE_URL .env.local)` was silently corrupting the URL (the value's surrounding quotes became literal characters in the env var), so drizzle-kit's `migrate` failed fast with its error text swallowed by the CLI's spinner — fixed by stripping the quotes explicitly before export. Added getStorefrontLayout/updateStorefrontLayout and getStylePresets/updateStylePresets to queries.ts, all following the updateCatalogAccess/updateCatalogPromoOverride field-scoped partial-update pattern. Repointed LayoutMatrix.tsx, SpotlightLayout.tsx (featuredProductId), both exports of StyleCustomizer.tsx, AnnouncementBar.tsx, and admin.tsx's StorefrontLayoutSettings; deleted the now-dead getActiveLayout/setActiveLayout/getFeaturedProductId/setFeaturedProductId (layoutMatrix.ts) and getStylePresets/saveStylePresets/getAnnouncementConfig/saveAnnouncementConfig (stylePresets.ts) after confirming zero remaining runtime importers. Correction to Session 51's notes: its "live-verified homepage" claim actually exercised EvaluationScaffold's hardcoded sample data, not the real catalog chain — LayoutMatrix.tsx's storeOperationalStatus has been hardcoded to "demo" all along, so SpotlightLayout/MagazineLayout/ClassicGridLayout are unreachable via normal routing today, and AnnouncementBar.tsx turns out not to be mounted anywhere either (same class of gap as AIChatbot.tsx). To get real signal this session, storeOperationalStatus was temporarily flipped to "live", AnnouncementBar was temporarily mounted on the homepage, and the storefront\_layout row was temporarily set to "spotlight" via direct SQL — all three new DB reads (getStorefrontLayout, getCatalogItems, getStylePresets) confirmed working end to end against real Neon data. Every temporary change was reverted and the storefront\_layout row deleted (reinitializes to defaults on next read) before committing; repo diff is migration-only. tsc --noEmit re-confirmed clean against the same known baseline, re-run with translations-test.ts excluded per last session's finding — no new errors. Noticed but left alone as out of scope: a pre-existing double-dollar-sign bug in the default shipping announcement text ("...over $$50!" — AnnouncementBar.tsx's replace() logic collides with a literal "$" already in DEFAULT\_ANNOUNCEMENT.shippingMessage) and several untranslated i18n keys on the spotlight layout. StyleCustomizer.tsx and StorefrontLayoutSettings (admin.tsx) were verified by code review and tsc only, per standing no-admin-credentials decision.
* Session 53: tsc-masking gate fix (phase 1) — deleted the orphaned, unimported src/data/translations-test.ts whose parse failure was aborting tsc's semantic diagnostics project-wide, and removed 30 unused-declaration errors (TS6133) it had been masking. No behavior change; real error count drops from 55 (with the file excluded) to 25. Remaining 25 need their own triage/decision sessions (deploy-path staleness in serve.ts/vercel-entry.ts, mergeAnonymousUser.ts Drizzle typing, and a handful of scattered type mismatches).
* Session 53: Removed dead cto.new prototype scaffold (serve.ts, vercel-entry.ts, build-vercel.sh, publish.sh, go-live.sh, SITE.md) and their package.json scripts/.gitignore entry — confirmed unused since the project moved off the original AI-app-builder platform to a GitHub/Vercel-managed workflow; the site's real Vercel deploy path (Nitro vercel preset in vite.config.ts) is untouched and still builds clean. No behavior change. Clears 8 of the 25 tsc errors surfaced in the previous cleanup; 17 remain, tracked separately.
* Session 53: tsc triage, Cluster 3 — fixed 8 scattered pre-existing type errors, three of which were live bugs rather than just type noise: NavBar.tsx's branding name always fell back to a hardcoded default (wrong property name, now reads the configured store name), and two ringColor style overrides (admin.tsx, ProgressTracker.tsx) were silently no-ops (invalid CSS property name, now set via the real --tw-ring-color custom property). Also deleted the dead, unimported LocaleContext.tsx, removed two no-op props/config left over from boilerplate in __root.tsx, and fixed two minor type-strictness nits (Footer.tsx, recoveryCrypto.ts). No other behavior change. tsc --noEmit now shows exactly 9 remaining errors, all in mergeAnonymousUser.ts, deliberately left for its own onLinkAccount merge-logic session.
* Session 56: IDOR + wallet-trust fixes (Task 1 of 4) — dbSubmitRefundClaim (refunds.ts) no longer trusts client-supplied downloadId/productId/productTitle/transactionId: it now looks up the real downloads row server-side and rejects unless download.userId matches the caller, deriving productId/productTitle/transactionId (download.sessionId) from that row instead; a buyer could previously name someone else's downloadId and have it refunded/access-revoked on approval. saveWallet/dbSaveWallet deleted outright (pre-flight found zero real callers besides its own definition — AIChatbot.tsx/CreditWidget.tsx/UserControlCenter.tsx/admin.tsx only ever called getWallet/addCredits/deductCredits/getCostPer1K/saveCostPer1K), closing the hole where a buyer could set their own credits/totalPurchased/totalConsumed to any value; addCredits/deductCredits (the only legitimate credit-mutation paths) are untouched. costPer1K moved off the per-user wallet row into a new global credit_settings table (id "global", one flat rate for every visitor) — same insert-with-onConflictDoNothing-on-first-read / admin-gated onConflictDoUpdate pattern as affiliateSettings.ts; previously dbSaveCostPer1K wrote to the admin's own wallet row, which no buyer's dbGetCostPer1K ever read, so the admin "Cost per 1K tokens" setting did nothing before this fix. Migration 0018_clear_dust.sql (additive CREATE TABLE credit_settings + DROP COLUMN wallet.cost_per_1k) applied to Neon and confirmed via drizzle.__drizzle_migrations and information_schema, not just drizzle-kit's exit code. refundLoyaltyPoints and RefundForm.tsx left untouched per scope. Live-tested end to end against two real test-buyer accounts and a real Stripe test-mode purchase (not simulated): confirmed the old client-controlled fields are ignored, confirmed a same-shaped IDOR attempt (spoofed productId/productTitle/transactionId, real victim downloadId) is rejected with zero refund_claims row created, confirmed costPer1K reads/writes are now truly global across buyers, confirmed saveWallet no longer exists as an export, confirmed addCredits/deductCredits still work, and confirmed the full approve path (real Stripe refund, download.status -> refunded, refund_claims.status -> approved) and reject path (no Stripe call, download untouched) both still work. Noticed but out of scope: dbGetRefundClaims (used by the admin Refund Claims Manager) is scoped by the caller's own userId, so the admin dashboard's refund list only ever shows the signed-in admin's own claims, not all buyers' pending claims — pre-existing, not part of this task's audit list, flagged for its own fix. tsc --noEmit matches the 9-error mergeAnonymousUser.ts baseline exactly.
* Session 56: Media delivery route negative-path security testing (Task 2 of 4) — live-tested /api/media/$productId (src/routes/api/media.$productId.ts) against a real Stripe-purchased download and a real private Blob file (588-byte test PDF). Pre-flight confirmed the route, mediaAccess.ts's hasPurchased(), and getUserId.ts all still matched their documented behavior at HEAD, unchanged by Task 1. Results: (1) control — owning buyer, valid session: 200, application/pdf, 588 bytes, first-20-bytes match the real Blob file exactly. (2) wrong user — non-owning buyer, valid session: 403, 9-byte "Forbidden" body, zero bytes of the real file. (3) unauthenticated — no session cookie at all: was a 500 (uncaught "No session — anonymous sign-in should have run on app load" from getUserId()), confirming the flagged risk; fixed by wrapping only this route handler's getUserId() call in try/catch, returning a plain 401 "Unauthorized" on failure — getUserId.ts itself is untouched, so every other caller still gets the throw it relies on. Re-tested after the fix: clean 401. (4) cache headers — the real response carries no Cache-Control or ETag, but does carry Vercel's own x-vercel-cache: MISS even in local dev, meaning Vercel's edge layer actively evaluates this route for caching; recommend adding Cache-Control: private, no-store, not applied per the task's out-of-scope list. No deployed Vercel preview URL exists for this branch to cross-check case 4 there. hasPurchased() and Range/206 support untouched. tsc --noEmit matches the 9-error baseline.
* Session 56: CSP / security headers + local sample media (Task 3 of 4) — enforcing Content-Security-Policy on every request, implemented as TanStack Start global request middleware (new src/start.ts, createStart/createMiddleware from @tanstack/react-start), not Nitro routeRules or vercel.json: it's the only layer that runs identically in `vite dev` and the built app, and it's the only place able to generate one nonce per request and thread it to both the response header and the page's injected scripts. Pre-flight found the real Vercel Blob public-store hostname (halu8nj4tasr7erk.public.blob.vercel-storage.com, confirmed from a live upload — the project's two existing catalog items only had placeholder example.com URLs, so a real cover image was uploaded for this) baked into img-src/media-src; re-grepping src/ for external domains turned up one real finding beyond the known list — src/data/cloudSync.ts's Google Drive cloud-sync feature calls fetch() against www.googleapis.com client-side, which connect-src 'self' now blocks (its OAuth redirect itself is unaffected, only the Drive API calls after linking); left broken per the task's explicit "no Google OAuth/Drive allowances" scope, flagged here since it's a real regression, not a hypothetical one. Building for production and rendering the homepage found exactly one inline <script> (TanStack Start's own $tsr-stream-barrier hydration bootstrap) — rather than fall back to Report-Only, router.tsx now reads a per-request nonce off getGlobalStartContext() (set by the new middleware) and passes it as router `ssr.nonce`, which TanStack Start applies to every script/link tag it injects; verified in the rendered HTML that the header's nonce and every injected tag's nonce attribute match. style-src keeps 'unsafe-inline' (documented, scoped to EvaluationScaffold.tsx's one inline Google-Fonts <style> block — threading the same nonce that deep would need a context provider wired from the root, not a quick change). Nitro's own routeRules mechanism was confirmed capable of setting headers (it already sets Cache-Control on /assets/** by default) but wasn't used, per the reasoning above. Verified against a locally-built production bundle (.vercel/output, run directly through its own Vercel Node handler, not `vite preview`, which can't serve this preset's output) on top of a real Neon connection: homepage carries the CSP header with a fresh nonce every request and zero console violations; Merriweather/Playfair/Inter/Lora fonts report document.fonts status "loaded", not system fallback; the real Blob cover image renders with zero violations; a temporary no-media catalog item (deleted after) plays the new local placeholder audio/video (public/sample-media/sample-audio.mp3, sample-video.mp4 — generated locally with a temporary ffmpeg install, never committed) with zero requests to soundhelix.com or w3schools.com and its VTT captions still load via a blob: media-src URL; a full real Stripe test-mode purchase against the CSP build redirected to Stripe and back and produced a real downloads row. The admin-gated checks (three export downloads in CatalogDashboard.tsx/OffboardingCenter.tsx/StorageConsole.tsx, full admin dashboard load) had to be verified against the dev server instead of the standalone local prod-build harness — Better Auth's trustedOrigins is dev-only-branch-gated on NODE_ENV, which gets statically inlined to "production" by the build, eliminating that branch entirely, so no port except the one baked into BETTER_AUTH_URL can authenticate against a locally-run prod build; not a bug, matches real deployed behavior. All three exports and the full dashboard loaded with zero CSP violations once the dev server (which runs the same middleware unbundled) was restarted with a cleared node_modules/.vite cache — an unrelated, pre-existing Vite optimize-deps staleness issue that resurfaced twice this session, both times fixed the same way. `npm run dev` still loads the homepage with HMR connecting normally under the new policy. Not applied: worker-src (a stray "blob: worker blocked by script-src fallback" console error turned out to be stale state in one browser tab, not reproducible in a fresh tab or a fresh dev-server load — flagged here in case it recurs, but not treated as a real finding without reproduction). tsc --noEmit matches the 9-error baseline.
* Session 56: npm audit fix + Dependabot (Task 4 of 4) — pre-flight found the live count had drifted from Session 55's snapshot (10: 3 high/7 moderate) to 9 (3 high/6 moderate) at current HEAD, expected since npm audit reads the live GitHub Advisory feed rather than a fixed snapshot; flagged, not treated as a blocker. `npm audit fix --dry-run` showed only patch/minor bumps (update-browserslist-db, postcss, node-releases, nanoid, js-yaml, electron-to-chromium, drizzle-kit 0.31.10->0.31.11, caniuse-lite, browserslist, baseline-browser-mapping) — no major-version bumps, so the task proceeded. Ran `npm audit fix` (no --force): 9 -> 4 vulnerabilities, all 4 remaining are one esbuild advisory reachable only through an old @esbuild-kit/core-utils/esm-loader chain that only `npm audit fix --force` would resolve, by downgrading drizzle-kit to 0.18.1 — a real breaking change, correctly left alone per scope. Only package-lock.json changed (no direct dependency in package.json moved). Added .github/dependabot.yml (npm, weekly, PR limit 10) exactly as specified. tsc --noEmit matches the 9-error baseline; `npm run build` completes; dev server homepage smoke-tested clean with no console errors.
